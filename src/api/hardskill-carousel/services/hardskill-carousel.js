'use strict';

/**
 * hardskill-carousel service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::hardskill-carousel.hardskill-carousel');
